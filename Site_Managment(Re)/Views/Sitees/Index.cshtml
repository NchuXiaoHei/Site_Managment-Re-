@model IEnumerable<Site_Managment_Re_.Models.Sitee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Site_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Site_status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Note)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Site_status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Note)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Site_name }) |
            @Html.ActionLink("Details", "Details", new { id=item.Site_name }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Site_name })
        </td>
    </tr>
}

</table>
